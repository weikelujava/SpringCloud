

server:
  port: 6001

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        id: gateway-service


smartyang:
  gateway:
    # 网关动态路由
    dynamicRoute:
      enabled: true
      dataType: nacos